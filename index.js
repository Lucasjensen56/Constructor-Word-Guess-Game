






var words = [
    "Harry Potter",
    "Dobby",
    "Voldemort"
  ]; 
  
// generate random word
var randomWord = words[Math.floor(Math.random() * words.length)];
console.log(randomWord) 

var blankWord = "";

    
// loop through words
    for (i = 0; i < words.length; i++) {
      var arrayWords = words[i];
   };
    
 

// user presented with random word to guess

// display how many guess they have left

// if user gets right letter, display letter. if user get incorrect letter, take try away

